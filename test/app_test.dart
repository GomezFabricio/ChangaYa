import 'package:flutter_test/flutter_test.dart';

// Placeholder test — se reemplaza con tests reales a medida que se implementan features.
// El CI requiere al menos un test para ejecutar la suite.
void main() {
  group('ChangaYa', () {
    test('placeholder — suite inicializada', () {
      expect(true, isTrue);
    });
  });
}
